# Prisma reads DATABASE_URL from api/.env (Prisma CLI and the NestJS
# runtime both look for it next to prisma/schema.prisma).
# These values match the root .env.example used by compose.yml.
DATABASE_URL="postgresql://user:pass@localhost:12345/ahiahi?schema=public"
